# Deliberately vulnerable MCP server — official MCP SDK (FastMCP + Streamable HTTP) + Jinja2.
# Same real stack as the primary acme-mcp server; pinned for reproducibility.
mcp==1.28.1
uvicorn==0.49.0
jinja2>=3.1,<4
