# Real A2A agent — official Google A2A SDK with the Starlette HTTP server extra.
# Pinned for reproducibility; bump deliberately and re-prove against aipostex.
a2a-sdk[http-server]==1.1.0
uvicorn==0.49.0
# Used by the opt-in A2A_MODE=vuln fixture to dereference a caller-supplied peer/card
# URL (the confused-deputy + card-trust behaviours). a2a-sdk already pulls httpx in;
# pinned here so the deliberately-weak fixture build is self-describing.
httpx==0.28.1
